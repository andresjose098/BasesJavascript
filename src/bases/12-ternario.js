//Operador condicional ternario

const activo = true;

// let mensaje = '';

// if (activo) {
//     mensaje= 'activo';
// } else {
//     mensaje = 'Inactivo'
// }

//const mensaje = (!activo) ? 'Activo' : null;

//condicion forma corta if
const mensaje = activo===true && 'Activo'

console.log(mensaje);